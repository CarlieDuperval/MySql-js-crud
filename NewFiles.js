const displayName = (name)=>{
    console.log(name)
    return name 
}
const nameArr = ["Darie", "Ritchell", "Erique", "Harold"]
//diplayName(nameArr[0])
//diplayName(nameArr[1])
//diplayName(nameArr[2])
//diplayName(nameArr[3])



// Whrite a method that logs a list namesin terminal
const displayNamesFromArray = (array) =>{

for (let i = 0; i < Array.length; i++){
displayName(arr[i])
  
}
}
displayNamesFromArray([1,2,345, "Harold",] )